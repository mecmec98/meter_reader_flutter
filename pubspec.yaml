name: meter_reader_flutter
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.1.2
  flutter_svg: ^2.0.17

  file_picker: ^8.3.1
  sqflite: ^2.4.1
  path_provider: ^2.1.5
  path: ^1.9.0
  bluetooth_print_plus: ^2.4.5
  cupertino_icons: ^1.0.8
  flutter_esc_pos_utils: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  sqflite_common_ffi: ^2.3.2

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
    - assets/database/MRADB.dbi
